# Все текстовые файлы модуля хранятся и выгружаются в LF независимо от ОС —
# Magisk исполняет shell-скрипты на устройстве, CRLF ломает их. Это защита
# на уровне git в дополнение к нормализации в build.py.
* text=auto eol=lf
*.zip binary
*.png binary
